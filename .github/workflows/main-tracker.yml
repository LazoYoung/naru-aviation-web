name: Main Tracker
run-name: ${{ github.actor }} triggered this action.
on:
  push:
    branches:
      - main
jobs:
  fetch:
    name: Fetch
    runs-on: ubuntu-latest
    steps:
      - run: echo "Fetching updates from remote..."
      - uses: actions/checkout@v3
      - run: echo "Fetch complete."
