name: Main Tracker
run-name: ${{ github.actor }} triggered this action.
on:
  push:
    branches:
      - main
jobs:
  deploy:
    name: Deploy
    runs-on: [self-hosted, linux]
    steps:
      - run: chmod u+x ./deploy.sh
      - run: ./deploy.sh
